<UserControl x:Class="DeepfreezeApp.ArchiveView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:cal="http://www.caliburnproject.org"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <StackPanel>
            <Border Height="50" BorderBrush="AliceBlue" BorderThickness="1">
                <StackPanel 
                    HorizontalAlignment="Center" 
                    VerticalAlignment="Center">
                    
                    <TextBlock 
                        Text="Drag files here to start uploading a new archive"
                        />
                    
                    <TextBlock
                        Text="or choose a folder"
                        Cursor="Hand"
                        TextDecorations="Underline"
                        Foreground="Blue"
                        cal:Message.Attach="[Event MouseLeftButtonUp] = [Action ChooseFolder()]"
                        />
                
                </StackPanel>
            </Border>
        </StackPanel>
    </Grid>
</UserControl>
