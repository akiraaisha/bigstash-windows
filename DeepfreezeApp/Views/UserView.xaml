<UserControl x:Class="DeepfreezeApp.UserView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:cal="http://www.caliburnproject.org"
             xmlns:local="clr-namespace:DeepfreezeApp"
             xmlns:controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
             xmlns:gravatar="clr-namespace:Controls;assembly=Controls.Gravatar"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="350">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/Controls.Gravatar;component/generic.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <StackPanel Width="350">

        <Label x:Name="ActiveUserHeader"
               Style="{DynamicResource DescriptionHeaderStyle}"
               Margin="5 5 0 0"/>

        <Grid Margin="5 5 0 0"
              HorizontalAlignment="Stretch">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <gravatar:Gravatar 
                Grid.Row="0"
                Grid.Column="0"
                Grid.RowSpan="2"
                Width="70"
                Email="{Binding ActiveUser.Email}"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Stretch"
                />

            <Label 
                Grid.Row="0"
                Grid.Column="1"
                Content="{Binding ActiveUser.DisplayName}" 
                FontSize="14" 
                HorizontalAlignment="Left" 
                VerticalAlignment="Center"
                />

            <Label 
                Grid.Row="1"
                Grid.Column="1"
                Content="{Binding ActiveUser.Email}" 
                FontSize="14" 
                HorizontalAlignment="Left"
                VerticalAlignment="Center"
                />

            <Button 
                Grid.Row="2"
                Grid.Column="0"
                x:Name="Logout" 
                Content="{Binding LogoutString}"
                Width="70"
                HorizontalAlignment="Left"
                VerticalAlignment="Bottom"
                Style="{DynamicResource SquareButtonStyle}"
                Margin="0 10 0 0"
                />
        </Grid>
              
        <Label x:Name="QuotaHeader" 
                Style="{DynamicResource DescriptionHeaderStyle}" 
               Margin="5 10 0 0"
                />
                
            <!--<Button Height="30" Width="30" VerticalContentAlignment="Center" HorizontalContentAlignment="Center" 
                    HorizontalAlignment="Right" VerticalAlignment="Center" Style="{DynamicResource MetroCircleButtonStyle}"
                    x:Name="RefreshStats" ToolTip="Refresh" >
                <Rectangle Width="13" Height="13" Fill="Black">
                    <Rectangle.OpacityMask>
                        <VisualBrush Stretch="Uniform"  Visual="{DynamicResource appbar_refresh}" />
                    </Rectangle.OpacityMask>
                </Rectangle>
            </Button>-->
                

        <!--<Label Grid.Row="1" Grid.Column="2" x:Name="AvailableSpaceText" HorizontalAlignment="Left" VerticalAlignment="Center" />
        <ProgressBar Grid.Row="2" Grid.Column="2" Minimum="0" Maximum="100" 
                        Value="{Binding UsedSpacePercentage}" ToolTip="{Binding UsedSpaceText}" />-->
        

        

    </StackPanel>
</UserControl>
